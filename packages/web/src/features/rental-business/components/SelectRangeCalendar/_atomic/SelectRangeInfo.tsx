import styled from "styled-components";

const LabelWrapper = styled.div`
  font-size: 16px;
`;

const SelectRangeInfo: React.FC = () => (
  <div>
    <LabelWrapper>label</LabelWrapper>
  </div>
);

export default SelectRangeInfo;
