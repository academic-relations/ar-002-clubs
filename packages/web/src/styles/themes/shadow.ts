/**
 * @file   shadow.ts
 * @brief  Shadow constants for themes (light, dark, etc.) and components (button, etc.)
 * @author dora@sparcs.org (Jiyoon Lee)
 */

const shadow = {
  md: "-1px 2px 4px 0px rgba(0, 0, 0, 0.25)",
};

export default shadow;
