/**
 * @file   round.ts
 * @brief  Round constants for themes (light, dark, etc.) and components (button, etc.)
 * @author dora@sparcs.org (Jiyoon Lee)
 */

const round = {
  sm: "4px",
  md: "8px",
};

export default round;
